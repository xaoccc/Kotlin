# Why Kotlin?

1. Complete null safety.
2. You can use all Java libraries + more.
3. Has coroutines (easy readable way to write async).
4. Less code than Java.

# Run Kotin code in terminal
`$kotlinc Main.kt -include-runtime -d Main.jar`  
`$java -jar Main.jar`
